displayName=TTSDialogue
author=Nobody
description=An tTSDialogue greeter plugin
tags=
plugins=com.grahambartley.TTSDialoguePlugin
# TODO(#31): when finalizing the Hub manifest, add a warning= line disclosing that with the Cloud
# (Azure) voice backend selected, dialogue text and the configured Azure region leave the machine
# and are sent to Microsoft Azure using the user-supplied subscription key. The local backends stay
# fully offline; this disclosure applies only to the cloud backend.